"""Host integration layer for OpenCode."""

# OpenCode plugin interface - commands are exposed through CLI
# This module provides OpenCode-specific extensions if needed

HOST_NAME = "opencode"


def get_host_commands():
    """Get host-specific commands for OpenCode."""
    return []
